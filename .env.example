# Copie para .env (que é gitignored) e preencha.
#
# Só é necessário se você quiser usar um modelo Claude via API como gerador
# (opção paga, não-local). O pipeline padrão roda 100% local sem isto.
ANTHROPIC_API_KEY=sk-ant-...
